<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >
    <item>
        <shape
            android:shape="rectangle">
            <!-- Set the border color of your layout here -->
            <stroke android:width="3dp" android:color="@color/iron" />
            <corners android:radius="10dp" />
            <solid android:color="@color/iron" />

        </shape>
    </item>

    <!-- This is for border bottom but
    you can change this according to your need -->
    <item android:bottom="4dp" >
        <shape
            android:shape="rectangle">
            <!-- Set the background color of your layout here -->
            <stroke android:width="2dp" android:color="@color/light" />
            <corners android:radius="10dp" />
            <solid android:color="@color/white" />
        </shape>
    </item>

</layer-list>